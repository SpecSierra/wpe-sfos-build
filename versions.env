# Current scripted baseline.
#
# These values are what the live build and packaging scripts use today. The
# previous 2.52.1 line is still available by override, but the default engine
# line is now 2.52.3. Use the newest public Sailfish SDK target here until the
# newer 5.1 sysroot becomes publicly downloadable.

SFOS_SYSROOT_VERSION=5.0.0.62

LIBWPE_VERSION=1.17.0
LIBEPOXY_VERSION=1.5.11
WPEBACKEND_FDO_VERSION=1.17.0

LEGACY_WPEWEBKIT_VERSION=2.52.3
LEGACY_QT5_PLUGIN_SOURCE_VERSION=2.52.1

WPE_SFOS_COMPAT_VERSION=1.0.0
ATLANTIC_BROWSER_VERSION=1.0.0

# Engine migration target.
TARGET_SFOS_VERSION=5.0.0.62
TARGET_WPEWEBKIT_VERSION=2.52.3

# Current public-baseline compatibility defaults.
# Keep the trimmed compatibility set explicit so it can be re-checked against
# device/runtime behavior while the build scripts target the newest public
# Sailfish SDK sysroot.
PATCH_GLIBC_VERSIONS=0
USE_GLIBC_COMPAT=0
USE_GLIB_COMPAT=0
USE_COW_STRING_COMPAT=0
USE_SIGILL_SKIP=1
USE_EGL_STUBS=1

# Historical only: the old packaging path used to build and depend on bubblewrap
# even though the WPE build disables bubblewrap sandboxing. Keep the version pin
# here for reference, but do not wire it into the default package graph.
BUBBLEWRAP_VERSION=0.11.0
